You are a robot manipulation expert analyzing an IKEA assembly manual step image to extract robot-executable actions.

=== STEP CONTEXT (provided by the assembly planner) ===
Assembly step index: {step_idx}
Parts being newly assembled in this step: {new_parts}
Existing subassembly (already assembled from prior steps): {subassembly_parts}
Known parts in the scene: {stage1_parts}
Step description from assembly plan: {stage2_step}

=== YOUR TASK ===
Analyze the manual image and extract structured actions following the three steps below.

--- STEP 1: IDENTIFY VISUAL CUES ---
Examine the image and answer:
- What motion arrows are visible? (straight = translational, curved = rotational, converging = press)
- Which numbered part labels are highlighted, circled, or shown moving?
- What is the direction of movement shown for each part? (downward, upward, horizontal, inward, at_angle)
- Does any part need to be rotated before connecting? (look for curved arrows on a part before it connects)

--- STEP 2: MAP ALL CONNECTIONS ---
For EVERY part in new_parts={new_parts}, determine what it connects to:
- Check each new part against EVERY part in subassembly_parts={subassembly_parts}
- Check each new part against every OTHER new part
- DO NOT stop at the first visible connection — list ALL connections you observe or can infer
- If new_parts is empty or subassembly_parts is empty, describe the connections among the visible parts

--- STEP 3: IDENTIFY EACH CONNECTION AND OUTPUT JSON ---

CONNECTION TYPES:
- INSERT: translational motion — one part slides/pushes into another (e.g., dowel into hole, leg into socket)
- PRESS: press together — parts pushed together until they snap or seat (e.g., snap fit, cam lock body)
- SCREW: rotational engagement — part is rotated to engage threads or a cam mechanism (e.g., screw, cam lock pin, nut, bolt)

PARAMETER VALUES:
- insertion_dir: "downward" | "upward" | "horizontal" | "inward" | "at_angle"

OUTPUT FORMAT — respond ONLY with valid JSON, no markdown fences, no extra text:
{
  "reasoning": "<brief summary of visual cues observed in STEP 1>",
  "connections": [
    {
      "active_part": <int>,
      "passive_part": <int>,
      "primitive": "INSERT" | "PRESS" | "SCREW",
      "insertion_dir": "<value>",
      "needs_reorient": true | false,
      "reorient_description": "<string or null>"
    }
  ]
}

IMPORTANT:
- active_part is the part that moves (the part being picked up and inserted/pressed/screwed). passive_part is the stationary receiving part.
- active_part must be from new_parts={new_parts}. passive_part can be from new_parts or subassembly_parts={subassembly_parts}.
- List ALL connections visible in the image — one entry per part-pair that joins
- Only use integer part IDs that appear in new_parts or subassembly_parts
- needs_reorient: set true only if the image shows a curved arrow or explicit rotation of the active_part before insertion
- reorient_description: describe in plain language what visual change is happening (e.g. "leg rotated from horizontal to vertical")
- Use SCREW for any connection where the manual shows a rotation arrow on a fastener (cam lock pin, screw, bolt) — do not classify these as INSERT
