You are a robot manipulation expert analyzing an IKEA assembly manual step image to extract robot-executable actions and connections for PDDL planning.

=== STEP CONTEXT (provided by the assembly planner) ===
Assembly step index: {step_idx}
Parts being newly assembled in this step: {new_parts}
Existing subassembly (already assembled from prior steps): {subassembly_parts}
Known parts in the scene: {stage1_parts}
Step description from assembly plan: {stage2_step}

=== YOUR TASK ===
Analyze the manual image and extract structured actions following the three steps below.

--- STEP 1: IDENTIFY VISUAL CUES ---
Examine the image and answer:
- What motion arrows are visible? (straight = translational, curved = rotational, converging = press)
- What tool icons appear? (screwdriver, Allen/hex key, cam-lock key, mallet, none)
- What connector symbols appear? (click/snap, force arrow, thread symbol, dowel hole)
- Which numbered part labels are highlighted, circled, or shown moving?
- Is there a close-up callout showing a fastener type? (cam lock, wood screw, dowel peg, bolt)

--- STEP 2: MAP ALL CONNECTIONS ---
For EVERY part in new_parts={new_parts}, determine what it connects to:
- Check each new part against EVERY part in subassembly_parts={subassembly_parts}
- Check each new part against every OTHER new part
- DO NOT stop at the first visible connection — list ALL connections you observe or can infer
- If new_parts is empty or subassembly_parts is empty, describe the connections among the visible parts

--- STEP 3: CLASSIFY EACH CONNECTION AND OUTPUT JSON ---

ACTION TAXONOMY:
- GRASP: pick up a new part before connecting it → parameters: part_id (integer)
- INSERT: translational peg-in-hole connection → parameters: peg_part_id, socket_part_id, insertion_dir, fit_type
- FASTEN: tool-mediated tightening → parameters: fastener_part_id, hole_part_id, tool, connection_type
- PRESS: press-fit or snap-fit coupling → parameters: part1_id, part2_id, press_dir, fit_type
- REORIENT: change orientation of a part before connecting → parameters: part_id, description

PARAMETER VALUES:
- insertion_dir / press_dir: "downward" | "upward" | "horizontal" | "inward" | "at_angle"
- fit_type (INSERT): "dowel" | "cam_lock_pin" | "shelf_pin" | "friction_fit"
- fit_type (PRESS): "snap_fit" | "press_fit"
- tool (FASTEN): "allen_key" | "phillips_screwdriver" | "flat_screwdriver" | "cam_tool" | "none"
- connection_type (FASTEN): "cam_lock" | "wood_screw" | "bolt_nut" | "clip"

CONNECTIONS_FORMED — map each connection to a PDDL predicate:
- INSERT action → predicate: "inserted",  part1: peg_part_id,      part2: socket_part_id
- FASTEN action → predicate: "fastened",  part1: fastener_part_id, part2: hole_part_id
- PRESS action  → predicate: "pressed",   part1: part1_id,         part2: part2_id

OUTPUT FORMAT — respond ONLY with valid JSON, no markdown fences, no extra text:
{
  "reasoning": "<brief summary of visual cues observed in STEP 1>",
  "actions": [
    {"primitive": "GRASP", "part_id": <integer>},
    {"primitive": "INSERT", "peg_part_id": <int>, "socket_part_id": <int>, "insertion_dir": "<value>", "fit_type": "<value>"},
    {"primitive": "FASTEN", "fastener_part_id": <int>, "hole_part_id": <int>, "tool": "<value>", "connection_type": "<value>"},
    {"primitive": "PRESS", "part1_id": <int>, "part2_id": <int>, "press_dir": "<value>", "fit_type": "<value>"},
    {"primitive": "REORIENT", "part_id": <int>, "description": "<value>"}
  ],
  "connections_formed": [
    {"predicate": "inserted"|"fastened"|"pressed", "part1": <int>, "part2": <int>}
  ]
}

IMPORTANT:
- Include only actions that are clearly shown or strongly implied in the image
- Use part IDs from the numbered overlays in the image (match to new_parts and subassembly_parts)
- List one GRASP per new part that must be picked up
- connections_formed must cover ALL connections formed in this step — one entry per part-pair that joins
- REORIENT and PLACE actions do NOT generate a connections_formed entry
- Only use integer part IDs that appear in new_parts or subassembly_parts
